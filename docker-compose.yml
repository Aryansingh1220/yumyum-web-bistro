version: '3.8'

services:
  web:
    image: aryansingh11/yumyum-restaurant:latest
    container_name: yumyum-web
    restart: always
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - yumyum-network

networks:
  yumyum-network:
    driver: bridge 